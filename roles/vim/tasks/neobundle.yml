---
- name: create ~/.vim/bundle
  file: >
    state=directory
    recurse=yes
    path=~/.vim/bundle

- name: git clone neobundle
  git: >
    repo=https://github.com/Shougo/neobundle.vim.git
    dest=~/.vim/bundle/neobundle.vim

- name: bundle clean
  command: /Applications/MacVim.app/Contents/MacOS/mvim -c NeoBundleClean -c q

- name: bundle install
  command: /Applications/MacVim.app/Contents/MacOS/mvim -c NeoBundleInstall -c q
